<Window x:Class="TestBrightness.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestBrightness"
        mc:Ignorable="d"

        Title="Brightness Control" Height="450" Width="800" Loaded="Window_Loaded" SizeToContent="WidthAndHeight">

	<Grid Margin="2">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>

		<TabControl x:Name="tabControl" Grid.Column="0" ItemsSource="{Binding MonitorSettings}" Margin="2">
			<TabControl.ItemTemplate>
				<DataTemplate>
					<TextBlock Grid.Column="0" x:Name="_HeaderText" Text="{Binding DeviceName}" />
				</DataTemplate>
			</TabControl.ItemTemplate>
			<TabControl.ContentTemplate>
				<DataTemplate>
				</DataTemplate>
			</TabControl.ContentTemplate>
		</TabControl>

		<local:WMIBrightnessCtrl x:Name="WMIBrightnessCtrl" Grid.Column="1" DataContext="{Binding}" />
	</Grid>
</Window>